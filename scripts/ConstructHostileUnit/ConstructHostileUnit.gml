/// @description ConstructHostileUnit(walkSpeed) Sets up the relevant variables for a hostile unit
/// @arg walkSpeed the walk speed of the hostile unit
var walkSpeed = argument[0];


ConstructUnit(walkSpeed);